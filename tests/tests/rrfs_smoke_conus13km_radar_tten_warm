###############################################################################
#
#  HRRR smoke physics on 13km domain with radar-tten, control run
#
###############################################################################

# This configuration is supposed to match the rrfs_dev1 parallel, so
# do not change the namelist without checking the current parallel.

export TEST_DESCR="HRRR smoke physics on 13km domain with radar-tten, control run"

export CNTL_DIR=rrfs_smoke_conus13km_radar_tten_warm

export LIST_FILES="sfcf000.nc \
                   sfcf001.nc \
                   sfcf002.nc \
                   atmf000.nc \
                   atmf001.nc \
                   atmf002.nc"

export_fv3
export_conus13km
export_rrfs_sd

export RESTART_INTERVAL=1
export FH_DFI_RADAR='0.0, 0.25, 0.50, 0.75, 1.0'
