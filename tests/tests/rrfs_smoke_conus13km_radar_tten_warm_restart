###############################################################################
#
#  HRRR smoke physics on 13km domain with radar-tten, restart run
#
###############################################################################

# This configuration is supposed to match the rrfs_dev1 parallel, so
# do not change the namelist without checking the current parallel.

export TEST_DESCR="HRRR smoke physics on 13km domain with radar-tten, restart run"

export CNTL_DIR=rrfs_smoke_conus13km_radar_tten_warm

export LIST_FILES="sfcf002.nc \
                   atmf002.nc"

export_fv3
export_conus13km
export_rrfs_sd

export FH_DFI_RADAR='0.0, 0.25, 0.50, 0.75, 1.0'

export FHROT=1
export RESTART_FILE_PREFIX=$( printf %04d%02d%02d.%02d0000 $SYEAR $SMONTH $SDAY $(( SHOUR+FHROT )) )
export RRFS_RESTART=YES
